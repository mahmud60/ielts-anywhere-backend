[build]
builder = "dockerfile"

[deploy]
startCommand = "celery -A app.tasks.grading.celery_app worker --loglevel=info"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
